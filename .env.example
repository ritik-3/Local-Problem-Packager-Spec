# Local Problem Packager environment defaults
# Keep offline mode enabled by default.
LPP_OFFLINE_MODE=true

# Optional local Ollama host override
OLLAMA_HOST=http://127.0.0.1:11434

# App data retention in days
LPP_AUDIT_RETENTION_DAYS=180
